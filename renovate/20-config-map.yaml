apiVersion: v1
kind: ConfigMap
metadata:
  name: renovate-config
  namespace: renovate
data:
  config.js: |
    module.exports = {
      platform: 'github',
      repositories: ['cassiechew/homelab'],
      onboarding: false,
      requireConfig: 'optional',
    };

