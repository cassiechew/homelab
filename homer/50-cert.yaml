apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homer-tls-prod-cert
  namespace: homer
spec:
  secretName: homer-tls-secret
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - homer.czcx.cc
