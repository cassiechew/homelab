apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: monica-access-policy
  namespace: monica
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: waypoint
  action: ALLOW
  rules:
    - from:
      - source:
          namespaces: ["istio-system", "monica"]
